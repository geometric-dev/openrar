#v1 .24.0 Implementation Plan(Revision 1) — Extraction Containment& Integrity

**Basis : **the product owner's Deep Research Analysis (Revision 8), parsed critically
              per instruction — not taken as gospel.This plan records the verdicts(adopt / revise /
                                                                                   reject),
    the revised architecture, the Failure - Mode Matrix, the named Negative - Test Plan,
    and the milestones.Normative inputs : `docs / SECURITY_ARCHITECTURE.md` §3 /§4 /§7,
    the v1 .24 pre - analysis, `docs / ROADMAP.md` §v1 .24.0.

                                           ##0. Parse verdicts on the pre -
                                       analysis

                                   | Pre - analysis item | Verdict | Notes | | : -- - | : -- - |
    : -- - | | §4.1 syscall containment(openat2 / write - through - handle) | **ADOPT,
    revised** | Revise : call `openat2` via `syscall()` directly — glibc wrappers landed in 2.36 and
        CI / WSL run older glibc.Replace the "bounded ancestor dirfd stack" with a per
                - file containment walk plus a** bounded LRU directory cache**(
                      simpler, same amortization;
                      re - anchor from the pinned root dirfd on cache eviction)
                      .|
            | §3.3 atomic extraction + journal | **ADOPT** |
            The journal - with - advisory -
                lock design(per - instance journals, lock - on - sweep, skip locked) is sound.Future
                - noted : Linux `O_TMPFILE` + `linkat(AT_EMPTY_PATH)` eliminates crash orphans
                          entirely — do not implement in v1
                          .24(the journal covers all platforms uniformly); record as a follow-up. |
| §3.2 collision detection (4 classes) | **ADOPT, revised** | REJECT the ICU dependency — vendored, generated tables instead (Unicode 15.1 pinned, generator script checked in, tables committed; size budget noted). Simple Case Folding table (~1.4k entries) + NFC composition table. |
| §4.4 encoding/trimming/UI invariant | **ADOPT** | Trim before BOTH UI rendering and path evaluation (CVE-2023-38831 class). Percent-encoding for undecodable names. |
| §3.1 JSON summary + stream separation | **ADOPT** | `--json-summary[=path]`, schema_version 1, stdout reserved for JSON. |
| §4.2 link semantics | **ADOPT** | Default deny;
decoupled opt - in;
session - scoped hardlinks;
SessionLinkRegistry per extract - all.| | §4.7 multi - volume containment | **REJECT the framing,
    keep the goal ** |
        The reader ALREADY scans all volumes and
            builds the complete merged entry list during `open()` — extraction is already
        "two-pass".The collision detector simply runs on the existing entries list;
**no pipeline change, no added latency **.REJECT the encrypted - header complexity
    : without the password nothing is enumerable or
                          extractable; with it, full detection runs. DEFER `UCM_PRESCAN` (additive API, not needed for containment correctness). |
| §4.8 WASM scope | **ADOPT** | Collision detection + per-entry status in scope;
filesystem containment N / A(in - memory).| | §4.9 metadata fidelity |
    **ADOPT** | SUID / SGID / sticky masking + umask bounding;
hardlink fallback with byte - cap debit;
unknown - extra preservation.| | §4.10 deferred dir metadata | **ADOPT** |
    Correct analysis(mtime clobbering + read - only lockout);
bottom - up restoration stack.| | 8.3 alias rejection | **ADOPT,
    with note ** | False - positive cost : user files legitimately named `NAME ~X.ext` are skipped -
                       with - report,
    not silently mangled.Acceptable;
rare.| | "child-process restriction"(v1 .23 carryover) | already rejected | Recorded in v1 .23;
unchanged.|

    **Gaps found beyond the pre -
        analysis : **

                   1. *
                   *Rename -
        target TOCTOU is closed by the atomic -
        rename design** —
   `FileRenameInformationEx` with POSIX semantics replaces the destination leaf* without following*
        it(a planted symlink at the destination is replaced, not traversed);
POSIX `renameat2(
    RENAME_NOREPLACE)` anchored to the parent `dirfd` likewise.Document as a designed property.2. *
        *Performance of the containment walk** : per
    - file component - by - component opens cost O(depth)
per
    file.A bounded LRU cache of verified directory handles(
            default 64 entries,
            re - anchor from the pinned root on eviction) amortizes common trees.Under FD pressure,
    evict first(the cache is a cache, not an invariant).3. *
            *Unicode table budget ** : generated tables must fit a documented size
                                       budget(~200 KiB committed) and
        the Unicode version must appear in the collision -
            detector header and the JSON summary metadata.4. *
                *Service headers are excluded from collisions ** — CMT / QO
                / RR service entries never collide with user
            - visible entries;
the detector runs on the
   final merged entry list with services filtered.
5. **`openat2` availability probing** must be cached per process (the WSL1
   leg exercises the fallback by construction).

## 1. Milestones

- **M1 — Atomic extraction foundation (§3.3)**: temp-in-destination writes
  (crypto-random `filename.<random>.tmp`), `FileRenameInformationEx`
  POSIX-semantic rename with `MoveFileExW` fallback (documented non-atomic on
  non-NTFS), `renameat2(RENAME_NOREPLACE)` / `renamex_np(RENAME_EXCL)` /
  `link()+unlink()` cascade on POSIX, per-instance journal manifest with
  advisory locking and startup sweep, FD accounting. Tests: journal
  concurrency (two extractors racing the sweep), orphan cleanup, rename
  fallback paths.
- **M2 — Syscall containment (§4.1)**: pinned root `dirfd`/handle; Windows
  open-once reparse-point-rejecting walk + `GetFinalPathNameByHandle`
  assertion (both sides canonicalized to `\\?\`) + write-through-handle;
POSIX `openat2(RESOLVE_BENEATH | RESOLVE_NO_SYMLINKS)` with cached availability probe, `openat` walk fallback, `/ proc
                                                                                                                   /
                                                                                                                   self
                                                                                                                   /
                                                                                                                   fd` prefix
                                                                                                                   verification;
8.3 - shaped name rejection;
directory - creation race protocol.Tests : TOCTOU fault injection(gate 1) — pre
    - planted symlinks / junctions at every component,
    race - window probes,
    WSL1 fallback exercise.-
        **M3 — Collision detection(§3.2) * * : `collision_detector` over the final merged entry
    list(services filtered)
    : duplicate -
        identical,
simple - case -fold, NFC, file - vs - directory;
    whole - extraction abort with exit 2;
    wired into CLI, DLL,
    and WASM surfaces(structural check runs everywhere).Unicode tables generated and pinned.Tests:
collision matrix(gate 2).- **M4 — Encoding + JSON summary(§4.4 /§3.1) * * : trim - before - UI -
    and-eval,
    displayed ≡ extracted invariant test, percent - encoding of undecodable names,
    timestamp clamping, `--json - summary[= path]` with schema_version 1 and stream separation.Tests
    : displayed≡extracted,
    schema golden tests,
    pipeline - stdout purity.- **M5 — Links
        + metadata fidelity(§4.2 /§4.9 /§4.10) * * : default - deny links with decoupled opt - in,
    SessionLinkRegistry, hardlink fallback with byte - cap debit,
    SUID / SGID / sticky masking + umask bounding,
    deferred directory metadata restoration(bottom - up), unknown - extra preservation,
    mutation test debt.- Gate mapping : gate 1(TOCTOU fault injection)lands with M2;
gate 2(collision matrix)lands with M3.

    ##2. Failure
    - Mode Matrix(Pillar 7)

          Phases
    : W = containment walk,
      T = temp write, R = rename / commit, S = sweep, C = collision check,
      M = metadata restore.Classes : crafted archive, local attacker(TOCTOU), OOM / ENOSPC,
      privilege, cancellation, cross - volume / FAT32,
      malformed headers.

          | Phase \ Class | crafted archive | local attacker | OOM / ENOSPC | privilege
          | cancellation | FAT32 / SMB | malformed | | : -- - | : -- - | : -- - | : -- - | : -- - |
    : -- - | : -- - | : -- - | | W | reject escape / 8.3 / ADS | per - component reparse rejection;
write - through - handle closes race | n / a | open failures =
    skip + report | n / a | n / a | clamp at scan(existing) | | T | crypto - random names |
    journal - referenced temps only | ENOSPC ⇒ entry failed + report | dir - create failures =
        skip + report | abort ⇒ disposition + journal | temp - in - dest same volume ✓ | name caps |
        | R | no - clobber default | POSIX - semantics replace does not follow the leaf |
        rename fail ⇒ failed entry | readonly respected(§3.2) |
        cancel mid - rename ⇒ atomic(old or new) | MoveFileEx fallback documented | n / a | | S |
        journal - referenced only | locked journals skipped | best - effort | best - effort |
        n / a | best - effort | garbage journals ignored(checksum) | | C |
        all 4 classes ⇒ abort exit 2 | n / a | n / a | n / a | n / a | n / a |
        runs post - scan on clamped list | | M | clamped metadata | n / a | best - effort restore |
        skip + report | restore skipped | best - effort | malformed metadata ⇒ skip |

        ##3. Negative -
            Test Plan(named)

                1. `toctou_symlink_target_race` — symlink pre -
            planted at the write target;
containment / assertion must catch it(gate 1).2. `toctou_midpath_junction` — junction planted at an
              intermediate component;
open - once walk rejects(Windows), openat2 rejects(POSIX).3. `toctou_journal_sweep_race` — two
                                   concurrent extractors;
the sweep skips locked journals and
        never deletes live temps.4. `toctou_rename_leaf_symlink` — symlink
            at the rename destination is replaced(not followed) under POSIX
    - semantics rename.5. `openat2_unavailable_falls_back` — probe -
    forced fallback path passes the same containment suite(WSL1 leg)
            .6. `alias_83_rejected` — `PROGRA ~1 /
        x` style components are refused with reports.7. `collision_duplicate_identical_abort` — exit
        2,
    atomic no -
                partial state
                    .8. `collision_case_folded_abort` — `Readme` + `README` ⇒ abort
                                                                       .9. `collision_nfc_nfd_abort` — NFC
                +
                NFD `café` ⇒ abort
                    .10. `collision_file_vs_dir_abort` — `foo` + `foo
                                                                     /` ⇒ abort
                                                                          .11. `collision_service_headers_exempt` — CMT
                                                                     /
                                                                     QO never trigger
                                                                         .12. `collision_multivolume_span` — collision
                                                                     split across volumes caught
                                                                         .13. `overwrite_no_clobber_default` — existing
                                                                     target
                + no flag ⇒ skip +
                report.14. `overwrite_readonly_respected` — READONLY target never clobbered
                    .15. `json_stdout_purity` — `--json
                -
                summary` routes every human byte to stderr
                        .16. `displayed_equals_extracted` — trimmed
                    / escaped display name
            == disk name.17. `percent_encoded_undecodable` — invalid - UTF -
                   8 name ⇒ lossless `% XX` name on disk
                   + report.18. `suid_sgid_stripped` — archived 04755 mode extracts 0755 &
        umask - bounded.19. `hardlink_fallback_byte_cap` — fallback copies debit the byte cap;
excess aborts.20. `dir_metadata_deferred` — read - only dir +
    restored mtime after
        children.21. `unknown_extras_roundtrip` — unknown extra records survive
            mutation.22. `journal_orphan_cleanup` — killed run leaves temps;
next run sweeps only journal - referenced files
                                   .

                               ##4. Sequencing& budget

                                   Risk Register item 2 applies
    : M2 touches every write path.M1 and M3 are independently shippable increments;
M2 is the core refactor and must land with gate 1. WASM collision wiring rides M3.M5 /
    M6 are additive
        .Each milestone lands with its named negative tests green on all CI legs before the next
            starts.
